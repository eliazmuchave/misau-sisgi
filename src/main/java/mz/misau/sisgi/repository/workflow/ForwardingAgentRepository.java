package mz.misau.sisgi.repository.workflow;

import mz.misau.sisgi.entity.workflow.ForwardingAgent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ForwardingAgentRepository extends JpaRepository<ForwardingAgent, Long> {
}
